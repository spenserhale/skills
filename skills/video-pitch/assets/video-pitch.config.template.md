# Video pitch

Per-repo defaults for the `video-pitch` skill. Everything is optional; delete lines that do not apply.

- Product name: __PRODUCT__
- Default audience: customer success
- Brand: background `#0b1020`, accent `#5eb1ff`, text `#f5f7fb`, font `Inter, system-ui, sans-serif`
- Logo: `assets/brand/logo.svg`
- Demo base URL: `http://localhost:3000` (start with `__DEV_COMMAND__`)
- Logged-in state: run `__LOGIN_STEPS__` once and save to `work/auth.json` (git-ignored); never record against production data
- Narration: provider `auto`, voice `__VOICE__`
- Output folder: `pitch-output/`
- Where pitches get posted: __CHANNEL_OR_LINK__
